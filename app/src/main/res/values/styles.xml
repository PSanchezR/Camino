<resources>
    <color name="theme_primary">#FFFF8D</color>
    <color name="theme_dark">#1A237E</color>
    <color name="theme_accent">#82B1FF</color>
    <color name="theme_components">#448AFF</color>
    <color name="theme_wbackgound">#ffff00</color>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <!-- Para Material Design
                <item name="android:colorPrimary">@color/theme_primary</item>
                <item name="android:colorPrimaryDark">@color/theme_dark</item>
                <item name="android:navigationBarColor">@color/theme_primary</item>
                <item name="android:colorAccent">@color/theme_accent</item>
                <item name="android:colorControlActivated">@color/theme_components</item>
                <item name="android:colorControlNormal">@color/theme_accent</item>
                <item name="android:windowBackground">@color/theme_wbackgound</item> -->

        <!--  Para AppCompat -->
        <item name="colorPrimary">@color/theme_dark</item>
        <item name="colorPrimaryDark">@color/theme_components</item>
        <item name="colorAccent">@color/theme_accent</item>
        <!-- <item name="android:textColorPrimary">@color/theme_wbackgound</item> -->
         <item name="android:windowBackground">@color/theme_accent</item>

     </style>

 </resources>
